component concentration_sensor
    % Concentration Sensor (TL Custom)

    nodes
        A = customization.thermal_liquid_custom.thermal_liquid_custom; % A:left
    end

    outputs
        X = {0,"g/m^3"};
    end

    equations
        X == A.x;
    end

end