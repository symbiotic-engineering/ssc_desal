function pi = osmotic_pressure(i, x, M, R, T)
% Osmotic pressure via the van 't Hoff equation for ideal dilute solutions.
definitions
    pi = i * (x / M) * R * T;
    %pi = {value(x,'g/l')*0.741829 + value(x,'g/l')^2*0.00111004, 'bar'};
end
end
